# Testing
1. check if any provider doesnt exist. 
1. prompt fintuing. only 1 provider to exist in envguard.json

# Publishing to npm
# 3. Build before publishing
npm run build

# 4. Publish to npm
npm publish

if publish command fails: npm login